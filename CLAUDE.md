# dotfiles

macOS用の設定ファイル群。

## シンボリックリンク設定済み

以下は `~/.config/` などへのシンボリックリンクが設定済み:

- `ghostty/` - Ghostty terminal設定
- `karabiner/` - Karabiner-Elements設定
- `nvim/` - Neovim設定
- `nix/` - Nix/home-manager設定
