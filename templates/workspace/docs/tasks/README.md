# Task State

Use one document per non-trivial task. Record the goal, affected repositories,
decisions, current changes, verification results, blockers, and next concrete action.
Update at meaningful milestones rather than appending every tool invocation.
Never record secrets, raw sessions, or private reasoning traces.
